- Developed a modular AI assistant using Streamlit that supports dynamic switching between OpenAI (GPT-4o-mini) and Google Gemini (2.5
flash) models.
- Architected a router function to manage conversation history and handle API requests across different LLM providers.
- Implemented a decoupled JSON-based configuration system to manage model hyperparameters like temperature, top_p, and token limits.
- Secured sensitive credentials using environment variables (python-dotenv) and built a persistent session-state memory for seamless user
interactions.
